const SectionTitle = ({ title }) => {
  return (
    <span className="section-title uppercase font-medium text-cyan-400 tracking-widest mb-14 inline-block">
      {title}
    </span>
  );
};

export default SectionTitle;
